import CreatePotsService from './CreateCategoryService';

describe('CreatePots', () => {
  it('should be able to create a new pot', () => {
    

  });

  // it('should not be able to create two pot on the same title', () => {

  // });
});
